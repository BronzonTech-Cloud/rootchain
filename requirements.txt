# Core dependencies
# Allow pip to select a FastAPI release that depends on a patched Starlette.
fastapi>=0.68.1,<0.80
uvicorn==0.15.0
pydantic==1.8.2
# Require a patched Starlette to avoid known CVEs in 0.14.2
starlette>=0.14.3

# HTTP and API
httpx==0.28.1
requests==2.32.4
python-multipart==0.0.20
python-jose[cryptography]==3.3.0

# Environment and configuration
python-dotenv==0.19.0

# Payment integration
stripe==2.60.0

# Monitoring and metrics
prometheus-client==0.11.0
sentry-sdk[fastapi]==1.3.1

# Caching
redis==3.5.3

# Testing
pytest==8.4.2
pytest-asyncio==0.15.1
locust==2.34.0

# Additional dependencies
h11==0.16.0
httpcore==1.0.9
asgiref==3.10.0
charset-normalizer==2.0.12
cffi==1.17.1 